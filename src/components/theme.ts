import {createMuiTheme} from '@material-ui/core'

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#242F40'
        },
        secondary: {
            main: '#CCA43B',
            contrastText: '#FFFFFF'
        }
    }
})

export default theme